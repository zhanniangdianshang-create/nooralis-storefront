{
  "name": "nooralis-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "Nooralis LED bionic butterfly drone storefront with switchable checkout providers.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check api/_paypal.js && node --check api/_checkout.js && node --check api/_orders.js && node --check api/checkout/config.js && node --check api/orders/index.js && node --check api/paypal/config.js && node --check api/paypal/orders.js && node --check api/paypal/orders/[orderID]/capture.js && node --check api/healthz.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20"
  }
}
